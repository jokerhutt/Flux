// Auto-generated by cft from: lto.h
// May require manual edits.

///
===-- llvm-c/lto.h - LTO Public C Interface ---------------------*- C -*-===*\
|*                                                                            *|
|* Part of the LLVM Project, under the Apache License v2.0 with LLVM          *|
|* Exceptions.                                                                *|
|* See https://llvm.org/LICENSE.txt for license information.                  *|
|* SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception                    *|
|*                                                                            *|
|*===----------------------------------------------------------------------===*|
|*                                                                            *|
|* This header provides public interface to an abstract link time optimization*|
|* library.  LLVM provides an implementation of this interface for use with   *|
|* llvm bitcode files.                                                        *|
|*                                                                            *|
\*===----------------------------------------------------------------------===
///

#ifndef LLVM_C_LTO_H;
#def LLVM_C_LTO_H 1;
#import "llvm-c/ExternC.fx";
#import "llvm-c/Visibility.fx";
#ifdef __cplusplus
#import <cstddef>;
#else
#import <stddef.fx>;
#endif;
#import <sys/types.fx>;
#ifndef __cplusplus;
// #if !defined(_MSC_VER)
#ifdef _MSC_VER
#import <stdbool.fx>;
#else
///
MSVC in particular does not have anything like _Bool or bool in C, but we can
at least make sure the type is the same size.  The implementation side will
use C++ bool.
///

// typedef: lto_bool_t = byte
#endif;
#else
#endif;
///
*
* @defgroup LLVMCLTO LTO
* @ingroup LLVMC
*
* @{

///

uint LTO_API_VERSION = 30;
///
*
* \since prior to LTO_API_VERSION=3

///

// typedef: lto_symbol_attributes = lto_symbol_attributes
enum lto_symbol_attributes
{
    LTO_SYMBOL_ALIGNMENT_MASK = 31,
    LTO_SYMBOL_PERMISSIONS_MASK = 224,
    LTO_SYMBOL_PERMISSIONS_CODE = 160,
    LTO_SYMBOL_PERMISSIONS_DATA = 192,
    LTO_SYMBOL_PERMISSIONS_RODATA = 128,
    LTO_SYMBOL_DEFINITION_MASK = 1792,
    LTO_SYMBOL_DEFINITION_REGULAR = 256,
    LTO_SYMBOL_DEFINITION_TENTATIVE = 512,
    LTO_SYMBOL_DEFINITION_WEAK = 768,
    LTO_SYMBOL_DEFINITION_UNDEFINED = 1024,
    LTO_SYMBOL_DEFINITION_WEAKUNDEF = 1280,
    LTO_SYMBOL_SCOPE_MASK = 14336,
    LTO_SYMBOL_SCOPE_INTERNAL = 2048,
    LTO_SYMBOL_SCOPE_HIDDEN = 4096,
    LTO_SYMBOL_SCOPE_PROTECTED = 8192,
    LTO_SYMBOL_SCOPE_DEFAULT = 6144,
    LTO_SYMBOL_SCOPE_DEFAULT_CAN_BE_HIDDEN = 10240,
    LTO_SYMBOL_COMDAT = 16384,
    LTO_SYMBOL_ALIAS = 32768
};

///
*
* \since prior to LTO_API_VERSION=3

///

// typedef: lto_debug_model = lto_debug_model
enum lto_debug_model
{
    LTO_DEBUG_MODEL_NONE,
    LTO_DEBUG_MODEL_DWARF
};

///
*
* \since prior to LTO_API_VERSION=3

///

// typedef: lto_codegen_model = lto_codegen_model
enum lto_codegen_model
{
    LTO_CODEGEN_PIC_MODEL_STATIC,
    LTO_CODEGEN_PIC_MODEL_DYNAMIC,
    LTO_CODEGEN_PIC_MODEL_DYNAMIC_NO_PIC,
    LTO_CODEGEN_PIC_MODEL_DEFAULT
};

///
* opaque reference to a loaded object module
///

LLVMOpaqueLTOModule* as lto_module_t;

struct LLVMOpaqueLTOModule;
///
* opaque reference to a code generator
///

LLVMOpaqueLTOCodeGenerator* as lto_code_gen_t;

struct LLVMOpaqueLTOCodeGenerator;
///
* opaque reference to a thin code generator
///

LLVMOpaqueThinLTOCodeGenerator* as thinlto_code_gen_t;

struct LLVMOpaqueThinLTOCodeGenerator;
LLVM_C_EXTERN_C_BEGIN;
///
*
* Returns a printable string.
*
* \since prior to LTO_API_VERSION=3

///

cdecl lto_get_version() -> byte*;
///
*
* Returns the last error string or NULL if last operation was successful.
*
* \since prior to LTO_API_VERSION=3

///

cdecl lto_get_error_message() -> byte*;
///
*
* Checks if a file is a loadable object file.
*
* \since prior to LTO_API_VERSION=3

///

cdecl lto_module_is_object_file(byte* path) -> byte;
///
*
* Checks if a file is a loadable object compiled for requested target.
*
* \since prior to LTO_API_VERSION=3

///

cdecl lto_module_is_object_file_for_target(byte* path, byte* target_triple_prefix) -> byte;
///
*
* Return true if \p Buffer contains a bitcode file with ObjC code (category
* or class) in it.
*
* \since LTO_API_VERSION=20

///

cdecl lto_module_has_objc_category(void* mem, ulong length) -> byte;
///
*
* Checks if a buffer is a loadable object file.
*
* \since prior to LTO_API_VERSION=3

///

cdecl lto_module_is_object_file_in_memory(void* mem, ulong length) -> byte;
///
*
* Checks if a buffer is a loadable object compiled for requested target.
*
* \since prior to LTO_API_VERSION=3

///

cdecl lto_module_is_object_file_in_memory_for_target(void* mem, ulong length, byte* target_triple_prefix) -> byte;
///
*
* Loads an object file from disk.
* Returns NULL on error (check lto_get_error_message() for details).
*
* \since prior to LTO_API_VERSION=3

///

cdecl lto_module_create(byte* path) -> LLVMOpaqueLTOModule*;
///
*
* Loads an object file from memory.
* Returns NULL on error (check lto_get_error_message() for details).
*
* \since prior to LTO_API_VERSION=3

///

cdecl lto_module_create_from_memory(void* mem, ulong length) -> LLVMOpaqueLTOModule*;
///
*
* Loads an object file from memory with an extra path argument.
* Returns NULL on error (check lto_get_error_message() for details).
*
* \since LTO_API_VERSION=9

///

cdecl lto_module_create_from_memory_with_path(void* mem, ulong length, byte* path) -> LLVMOpaqueLTOModule*;
///
*
* Loads an object file in its own context.
*
* Loads an object file in its own LLVMContext.  This function call is
* thread-safe.  However, modules created this way should not be merged into an
* lto_code_gen_t using \a lto_codegen_add_module().
*
* Returns NULL on error (check lto_get_error_message() for details).
*
* \since LTO_API_VERSION=11

///

cdecl lto_module_create_in_local_context(void* mem, ulong length, byte* path) -> LLVMOpaqueLTOModule*;
///
*
* Loads an object file in the codegen context.
*
* Loads an object file into the same context as \c cg.  The module is safe to
* add using \a lto_codegen_add_module().
*
* Returns NULL on error (check lto_get_error_message() for details).
*
* \since LTO_API_VERSION=11

///

cdecl lto_module_create_in_codegen_context(void* mem, ulong length, byte* path, LLVMOpaqueLTOCodeGenerator* cg) -> LLVMOpaqueLTOModule*;
///
*
* Loads an object file from disk. The seek point of fd is not preserved.
* Returns NULL on error (check lto_get_error_message() for details).
*
* \since LTO_API_VERSION=5

///

cdecl lto_module_create_from_fd(int fd, byte* path, ulong file_size) -> LLVMOpaqueLTOModule*;
///
*
* Loads an object file from disk. The seek point of fd is not preserved.
* Returns NULL on error (check lto_get_error_message() for details).
*
* \since LTO_API_VERSION=5

///

cdecl lto_module_create_from_fd_at_offset(int fd, byte* path, ulong file_size, ulong map_size, long offset) -> LLVMOpaqueLTOModule*;
///
*
* Frees all memory internally allocated by the module.
* Upon return the lto_module_t is no longer valid.
*
* \since prior to LTO_API_VERSION=3

///

cdecl lto_module_dispose(LLVMOpaqueLTOModule* mod) -> void;
///
*
* Returns triple string which the object module was compiled under.
*
* \since prior to LTO_API_VERSION=3

///

cdecl lto_module_get_target_triple(LLVMOpaqueLTOModule* mod) -> byte*;
///
*
* Sets triple string with which the object will be codegened.
*
* \since LTO_API_VERSION=4

///

cdecl lto_module_set_target_triple(LLVMOpaqueLTOModule* mod, byte* triple) -> void;
///
*
* Returns the number of symbols in the object module.
*
* \since prior to LTO_API_VERSION=3

///

cdecl lto_module_get_num_symbols(LLVMOpaqueLTOModule* mod) -> uint;
///
*
* Returns the name of the ith symbol in the object module.
*
* \since prior to LTO_API_VERSION=3

///

cdecl lto_module_get_symbol_name(LLVMOpaqueLTOModule* mod, uint index) -> byte*;
///
*
* Returns the attributes of the ith symbol in the object module.
*
* \since prior to LTO_API_VERSION=3

///

cdecl lto_module_get_symbol_attribute(LLVMOpaqueLTOModule* mod, uint index) -> lto_symbol_attributes;
///
*
* Returns the number of asm undefined symbols in the object module.
*
* \since prior to LTO_API_VERSION=30

///

cdecl lto_module_get_num_asm_undef_symbols(LLVMOpaqueLTOModule* mod) -> uint;
///
*
* Returns the name of the ith asm undefined symbol in the object module.
*
* \since prior to LTO_API_VERSION=30

///

cdecl lto_module_get_asm_undef_symbol_name(LLVMOpaqueLTOModule* mod, uint index) -> byte*;
///
*
* Returns the module's linker options.
*
* The linker options may consist of multiple flags. It is the linker's
* responsibility to split the flags using a platform-specific mechanism.
*
* \since LTO_API_VERSION=16

///

cdecl lto_module_get_linkeropts(LLVMOpaqueLTOModule* mod) -> byte*;
///
*
* If targeting mach-o on darwin, this function gets the CPU type and subtype
* that will end up being encoded in the mach-o header. These are the values
* that can be found in mach/machine.h.
*
* \p out_cputype and \p out_cpusubtype must be non-NULL.
*
* Returns true on error (check lto_get_error_message() for details).
*
* \since LTO_API_VERSION=27

///

cdecl lto_module_get_macho_cputype(LLVMOpaqueLTOModule* mod, uint* out_cputype, uint* out_cpusubtype) -> byte;
///
*
* This function can be used by the linker to check if a given module has
* any constructor or destructor functions.
*
* Returns true if the module has either the @llvm.global_ctors or the
* @llvm.global_dtors symbol. Otherwise returns false.
*
* \since LTO_API_VERSION=29

///

cdecl lto_module_has_ctor_dtor(LLVMOpaqueLTOModule* mod) -> byte;
///
*
* Diagnostic severity.
*
* \since LTO_API_VERSION=7

///

// typedef: lto_codegen_diagnostic_severity_t = lto_codegen_diagnostic_severity_t
enum lto_codegen_diagnostic_severity_t
{
    LTO_DS_ERROR = 0,
    LTO_DS_WARNING = 1,
    LTO_DS_REMARK = 3,
    LTO_DS_NOTE = 2
};

///
*
* Diagnostic handler type.
* \p severity defines the severity.
* \p diag is the actual diagnostic.
* The diagnostic is not prefixed by any of severity keyword, e.g., 'error: '.
* \p ctxt is used to pass the context set with the diagnostic handler.
*
* \since LTO_API_VERSION=7

///

cdecl{}* lto_diagnostic_handler_t(lto_codegen_diagnostic_severity_t severity, byte* diag, void* ctxt) -> void;

///
*
* Set a diagnostic handler and the related context (void *).
* This is more general than lto_get_error_message, as the diagnostic handler
* can be called at anytime within lto.
*
* \since LTO_API_VERSION=7

///

cdecl lto_codegen_set_diagnostic_handler(LLVMOpaqueLTOCodeGenerator*, def{}*(lto_codegen_diagnostic_severity_t, byte*, void*) -> void, void*) -> void;
///
*
* Instantiates a code generator.
* Returns NULL on error (check lto_get_error_message() for details).
*
* All modules added using \a lto_codegen_add_module() must have been created
* in the same context as the codegen.
*
* \since prior to LTO_API_VERSION=3

///

cdecl lto_codegen_create() -> LLVMOpaqueLTOCodeGenerator*;
///
*
* Instantiate a code generator in its own context.
*
* Instantiates a code generator in its own context.  Modules added via \a
* lto_codegen_add_module() must have all been created in the same context,
* using \a lto_module_create_in_codegen_context().
*
* \since LTO_API_VERSION=11

///

cdecl lto_codegen_create_in_local_context() -> LLVMOpaqueLTOCodeGenerator*;
///
*
* Frees all code generator and all memory it internally allocated.
* Upon return the lto_code_gen_t is no longer valid.
*
* \since prior to LTO_API_VERSION=3

///

cdecl lto_codegen_dispose(LLVMOpaqueLTOCodeGenerator*) -> void;
///
*
* Add an object module to the set of modules for which code will be generated.
* Returns true on error (check lto_get_error_message() for details).
*
* \c cg and \c mod must both be in the same context.  See \a
* lto_codegen_create_in_local_context() and \a
* lto_module_create_in_codegen_context().
*
* \since prior to LTO_API_VERSION=3

///

cdecl lto_codegen_add_module(LLVMOpaqueLTOCodeGenerator* cg, LLVMOpaqueLTOModule* mod) -> byte;
///
*
* Sets the object module for code generation. This will transfer the ownership
* of the module to the code generator.
*
* \c cg and \c mod must both be in the same context.
*
* \since LTO_API_VERSION=13

///

cdecl lto_codegen_set_module(LLVMOpaqueLTOCodeGenerator* cg, LLVMOpaqueLTOModule* mod) -> void;
///
*
* Sets if debug info should be generated.
* Returns true on error (check lto_get_error_message() for details).
*
* \since prior to LTO_API_VERSION=3

///

cdecl lto_codegen_set_debug_model(LLVMOpaqueLTOCodeGenerator* cg, lto_debug_model) -> byte;
///
*
* Sets which PIC code model to generated.
* Returns true on error (check lto_get_error_message() for details).
*
* \since prior to LTO_API_VERSION=3

///

cdecl lto_codegen_set_pic_model(LLVMOpaqueLTOCodeGenerator* cg, lto_codegen_model) -> byte;
///
*
* Sets the cpu to generate code for.
*
* \since LTO_API_VERSION=4

///

cdecl lto_codegen_set_cpu(LLVMOpaqueLTOCodeGenerator* cg, byte* cpu) -> void;
///
*
* Sets the location of the assembler tool to run. If not set, libLTO
* will use gcc to invoke the assembler.
*
* \since LTO_API_VERSION=3

///

cdecl lto_codegen_set_assembler_path(LLVMOpaqueLTOCodeGenerator* cg, byte* path) -> void;
///
*
* Sets extra arguments that libLTO should pass to the assembler.
*
* \since LTO_API_VERSION=4

///

cdecl lto_codegen_set_assembler_args(LLVMOpaqueLTOCodeGenerator* cg, byte** args, int nargs) -> void;
///
*
* Adds to a list of all global symbols that must exist in the final generated
* code. If a function is not listed there, it might be inlined into every usage
* and optimized away.
*
* \since prior to LTO_API_VERSION=3

///

cdecl lto_codegen_add_must_preserve_symbol(LLVMOpaqueLTOCodeGenerator* cg, byte* symbol) -> void;
///
*
* Writes a new object file at the specified path that contains the
* merged contents of all modules added so far.
* Returns true on error (check lto_get_error_message() for details).
*
* \since LTO_API_VERSION=5

///

cdecl lto_codegen_write_merged_modules(LLVMOpaqueLTOCodeGenerator* cg, byte* path) -> byte;
///
*
* Generates code for all added modules into one native object file.
* This calls lto_codegen_optimize then lto_codegen_compile_optimized.
*
* On success returns a pointer to a generated mach-o/ELF buffer and
* length set to the buffer size.  The buffer is owned by the
* lto_code_gen_t and will be freed when lto_codegen_dispose()
* is called, or lto_codegen_compile() is called again.
* On failure, returns NULL (check lto_get_error_message() for details).
*
* \since prior to LTO_API_VERSION=3

///

cdecl lto_codegen_compile(LLVMOpaqueLTOCodeGenerator* cg, ulong* length) -> void*;
///
*
* Generates code for all added modules into one native object file.
* This calls lto_codegen_optimize then lto_codegen_compile_optimized (instead
* of returning a generated mach-o/ELF buffer, it writes to a file).
*
* The name of the file is written to name. Returns true on error.
*
* \since LTO_API_VERSION=5

///

cdecl lto_codegen_compile_to_file(LLVMOpaqueLTOCodeGenerator* cg, byte** name) -> byte;
///
*
* Runs optimization for the merged module. Returns true on error.
*
* \since LTO_API_VERSION=12

///

cdecl lto_codegen_optimize(LLVMOpaqueLTOCodeGenerator* cg) -> byte;
///
*
* Generates code for the optimized merged module into one native object file.
* It will not run any IR optimizations on the merged module.
*
* On success returns a pointer to a generated mach-o/ELF buffer and length set
* to the buffer size.  The buffer is owned by the lto_code_gen_t and will be
* freed when lto_codegen_dispose() is called, or
* lto_codegen_compile_optimized() is called again. On failure, returns NULL
* (check lto_get_error_message() for details).
*
* \since LTO_API_VERSION=12

///

cdecl lto_codegen_compile_optimized(LLVMOpaqueLTOCodeGenerator* cg, ulong* length) -> void*;
///
*
* Returns the runtime API version.
*
* \since LTO_API_VERSION=12

///

cdecl lto_api_version() -> uint;
///
*
* Parses options immediately, making them available as early as possible. For
* example during executing codegen::InitTargetOptionsFromCodeGenFlags. Since
* parsing shud only happen once, only one of lto_codegen_debug_options or
* lto_set_debug_options should be called.
*
* This function takes one or more options separated by spaces.
* Warning: passing file paths through this function may confuse the argument
* parser if the paths contain spaces.
*
* \since LTO_API_VERSION=28

///

cdecl lto_set_debug_options(byte** options, int number) -> void;
///
*
* Sets options to help debug codegen bugs. Since parsing shud only happen once,
* only one of lto_codegen_debug_options or lto_set_debug_options
* should be called.
*
* This function takes one or more options separated by spaces.
* Warning: passing file paths through this function may confuse the argument
* parser if the paths contain spaces.
*
* \since prior to LTO_API_VERSION=3

///

cdecl lto_codegen_debug_options(LLVMOpaqueLTOCodeGenerator* cg, byte*) -> void;
///
*
* Same as the previous function, but takes every option separately through an
* array.
*
* \since prior to LTO_API_VERSION=26

///

cdecl lto_codegen_debug_options_array(LLVMOpaqueLTOCodeGenerator* cg, byte**, int number) -> void;
///
*
* Initializes LLVM disassemblers.
* FIXME: This doesn't really belong here.
*
* \since LTO_API_VERSION=5

///

cdecl lto_initialize_disassembler() -> void;
///
*
* Sets if we should run internalize pass during optimization and code
* generation.
*
* \since LTO_API_VERSION=14

///

cdecl lto_codegen_set_should_internalize(LLVMOpaqueLTOCodeGenerator* cg, byte ShouldInternalize) -> void;
///
*
* Set whether to embed uselists in bitcode.
*
* Sets whether \a lto_codegen_write_merged_modules() should embed uselists in
* output bitcode.  This should be turned on for all -save-temps output.
*
* \since LTO_API_VERSION=15

///

cdecl lto_codegen_set_should_embed_uselists(LLVMOpaqueLTOCodeGenerator* cg, byte ShouldEmbedUselists) -> void;
///
* Opaque reference to an LTO input file
///

LLVMOpaqueLTOInput* as lto_input_t;

struct LLVMOpaqueLTOInput;
///
*
 * Creates an LTO input file from a buffer. The path
 * argument is used for diagnotics as this function
 * otherwise does not know which file the given buffer
 * is associated with.
 *
 * \since LTO_API_VERSION=24

///

cdecl lto_input_create(void* buffer, ulong buffer_size, byte* path) -> LLVMOpaqueLTOInput*;
///
*
 * Frees all memory internally allocated by the LTO input file.
 * Upon return the lto_module_t is no longer valid.
 *
 * \since LTO_API_VERSION=24

///

cdecl lto_input_dispose(LLVMOpaqueLTOInput* input) -> void;
///
*
 * Returns the number of dependent library specifiers
 * for the given LTO input file.
 *
 * \since LTO_API_VERSION=24

///

cdecl lto_input_get_num_dependent_libraries(LLVMOpaqueLTOInput* input) -> uint;
///
*
 * Returns the ith dependent library specifier
 * for the given LTO input file. The returned
 * string is not null-terminated.
 *
 * \since LTO_API_VERSION=24

///

cdecl lto_input_get_dependent_library(LLVMOpaqueLTOInput* input, ulong index, ulong* size) -> byte*;
///
*
* Returns the list of libcall symbols that can be generated by LTO
* that might not be visible from the symbol table of bitcode files.
*
* \since prior to LTO_API_VERSION=25

///

cdecl lto_runtime_lib_symbols_list(ulong* size) -> byte**;
///
*
* @} // endgoup LLVMCLTO
* @defgroup LLVMCTLTO ThinLTO
* @ingroup LLVMC
*
* @{

///

///
*
* Type to wrap a single object returned by ThinLTO.
*
* \since LTO_API_VERSION=18

///

struct LTOObjectBuffer
{
    byte* Buffer;
    ulong Size;
};

///
*
* Instantiates a ThinLTO code generator.
* Returns NULL on error (check lto_get_error_message() for details).
*
*
* The ThinLTOCodeGenerator is not intended to be reuse for multiple
* compilation: the model is that the client adds modules to the generator and
* ask to perform the ThinLTO optimizations / codegen, and finally destroys the
* codegenerator.
*
* \since LTO_API_VERSION=18

///

cdecl thinlto_create_codegen() -> LLVMOpaqueThinLTOCodeGenerator*;
///
*
* Frees the generator and all memory it internally allocated.
* Upon return the thinlto_code_gen_t is no longer valid.
*
* \since LTO_API_VERSION=18

///

cdecl thinlto_codegen_dispose(LLVMOpaqueThinLTOCodeGenerator* cg) -> void;
///
*
* Add a module to a ThinLTO code generator. Identifier has to be unique among
* all the modules in a code generator. The data buffer stays owned by the
* client, and is expected to be available for the entire lifetime of the
* thinlto_code_gen_t it is added to.
*
* On failure, returns NULL (check lto_get_error_message() for details).
*
*
* \since LTO_API_VERSION=18

///

cdecl thinlto_codegen_add_module(LLVMOpaqueThinLTOCodeGenerator* cg, byte* identifier, byte* dat, int length) -> void;
///
*
* Optimize and codegen all the modules added to the codegenerator using
* ThinLTO. Resulting objects are accessible using thinlto_module_get_object().
*
* \since LTO_API_VERSION=18

///

cdecl thinlto_codegen_process(LLVMOpaqueThinLTOCodeGenerator* cg) -> void;
///
*
* Returns the number of object files produced by the ThinLTO CodeGenerator.
*
* It usually matches the number of input files, but this is not a guarantee of
* the API and may change in future implementation, so the client should not
* assume it.
*
* \since LTO_API_VERSION=18

///

cdecl thinlto_module_get_num_objects(LLVMOpaqueThinLTOCodeGenerator* cg) -> uint;
///
*
* Returns a reference to the ith object file produced by the ThinLTO
* CodeGenerator.
*
* Client should use \p thinlto_module_get_num_objects() to get the number of
* available objects.
*
* \since LTO_API_VERSION=18

///

cdecl thinlto_module_get_object(LLVMOpaqueThinLTOCodeGenerator* cg, uint index) -> LTOObjectBuffer;
///
*
* Returns the number of object files produced by the ThinLTO CodeGenerator.
*
* It usually matches the number of input files, but this is not a guarantee of
* the API and may change in future implementation, so the client should not
* assume it.
*
* \since LTO_API_VERSION=21

///

cdecl thinlto_module_get_num_object_files(LLVMOpaqueThinLTOCodeGenerator* cg) -> uint;
///
*
* Returns the path to the ith object file produced by the ThinLTO
* CodeGenerator.
*
* Client should use \p thinlto_module_get_num_object_files() to get the number
* of available objects.
*
* \since LTO_API_VERSION=21

///

cdecl thinlto_module_get_object_file(LLVMOpaqueThinLTOCodeGenerator* cg, uint index) -> byte*;
///
*
* Sets which PIC code model to generate.
* Returns true on error (check lto_get_error_message() for details).
*
* \since LTO_API_VERSION=18

///

cdecl thinlto_codegen_set_pic_model(LLVMOpaqueThinLTOCodeGenerator* cg, lto_codegen_model) -> byte;
///
*
* Sets the path to a directory to use as a storage for temporary bitcode files.
* The intention is to make the bitcode files available for debugging at various
* stage of the pipeline.
*
* \since LTO_API_VERSION=18

///

cdecl thinlto_codegen_set_savetemps_dir(LLVMOpaqueThinLTOCodeGenerator* cg, byte* save_temps_dir) -> void;
///
*
* Set the path to a directory where to save generated object files. This
* path can be used by a linker to request on-disk files instead of in-memory
* buffers. When set, results are available through
* thinlto_module_get_object_file() instead of thinlto_module_get_object().
*
* \since LTO_API_VERSION=21

///

cdecl thinlto_set_generated_objects_dir(LLVMOpaqueThinLTOCodeGenerator* cg, byte* save_temps_dir) -> void;
///
*
* Sets the cpu to generate code for.
*
* \since LTO_API_VERSION=18

///

cdecl thinlto_codegen_set_cpu(LLVMOpaqueThinLTOCodeGenerator* cg, byte* cpu) -> void;
///
*
* Disable CodeGen, only run the stages till codegen and stop. The output will
* be bitcode.
*
* \since LTO_API_VERSION=19

///

cdecl thinlto_codegen_disable_codegen(LLVMOpaqueThinLTOCodeGenerator* cg, byte disable) -> void;
///
*
* Perform CodeGen only: disable all other stages.
*
* \since LTO_API_VERSION=19

///

cdecl thinlto_codegen_set_codegen_only(LLVMOpaqueThinLTOCodeGenerator* cg, byte codegen_only) -> void;
///
*
* Parse -mllvm style debug options.
*
* \since LTO_API_VERSION=18

///

cdecl thinlto_debug_options(byte** options, int number) -> void;
///
*
* Test if a module has support for ThinLTO linking.
*
* \since LTO_API_VERSION=18

///

cdecl lto_module_is_thinlto(LLVMOpaqueLTOModule* mod) -> byte;
///
*
* Adds a symbol to the list of global symbols that must exist in the final
* generated code. If a function is not listed there, it might be inlined into
* every usage and optimized away. For every single module, the functions
* referenced from code outside of the ThinLTO modules need to be added here.
*
* \since LTO_API_VERSION=18

///

cdecl thinlto_codegen_add_must_preserve_symbol(LLVMOpaqueThinLTOCodeGenerator* cg, byte* name, int length) -> void;
///
*
* Adds a symbol to the list of global symbols that are cross-referenced between
* ThinLTO files. If the ThinLTO CodeGenerator can ensure that every
* references from a ThinLTO module to this symbol is optimized away, then
* the symbol can be discarded.
*
* \since LTO_API_VERSION=18

///

cdecl thinlto_codegen_add_cross_referenced_symbol(LLVMOpaqueThinLTOCodeGenerator* cg, byte* name, int length) -> void;
///
*
* @} // endgoup LLVMCTLTO
* @defgroup LLVMCTLTO_CACHING ThinLTO Cache Control
* @ingroup LLVMCTLTO
*
* These entry points control the ThinLTO cache. The cache is intended to
* support incremental builds, and thus needs to be persistent across builds.
* The client enables the cache by supplying a path to an existing directory.
* The code generator will use this to store objects files that may be reused
* during a subsequent build.
* To avoid filling the disk space, a few knobs are provided:
*  - The pruning interval limits the frequency at which the garbage collector
*    will try to scan the cache directory to prune expired entries.
*    Setting to a negative number disables the pruning.
*  - The pruning expiration time indicates to the garbage collector how old an
*    entry needs to be to be removed.
*  - Finally, the garbage collector can be instructed to prune the cache until
*    the occupied space goes below a threshold.
* @{

///

///
*
* Sets the path to a directory to use as a cache storage for incremental build.
* Setting this activates caching.
*
* \since LTO_API_VERSION=18

///

cdecl thinlto_codegen_set_cache_dir(LLVMOpaqueThinLTOCodeGenerator* cg, byte* cache_dir) -> void;
///
*
* Sets the cache pruning interval (in seconds). A negative value disables the
* pruning. An unspecified default value will be applied, and a value of 0 will
* force prunning to occur.
*
* \since LTO_API_VERSION=18

///

cdecl thinlto_codegen_set_cache_pruning_interval(LLVMOpaqueThinLTOCodeGenerator* cg, int interval) -> void;
///
*
* Sets the maximum cache size that can be persistent across build, in terms of
* percentage of the available space on the disk. Set to 100 to indicate
* no limit, 50 to indicate that the cache size will not be left over half the
* available space. A value over 100 will be reduced to 100, a value of 0 will
* be ignored. An unspecified default value will be applied.
*
* The formula looks like:
*  AvailableSpace = FreeSpace + ExistingCacheSize
*  NewCacheSize = AvailableSpace * P/100
*
* \since LTO_API_VERSION=18

///

cdecl thinlto_codegen_set_final_cache_size_relative_to_available_space(LLVMOpaqueThinLTOCodeGenerator* cg, uint percentage) -> void;
///
*
* Sets the expiration (in seconds) for an entry in the cache. An unspecified
* default value will be applied. A value of 0 will be ignored.
*
* \since LTO_API_VERSION=18

///

cdecl thinlto_codegen_set_cache_entry_expiration(LLVMOpaqueThinLTOCodeGenerator* cg, uint expiration) -> void;
///
*
* Sets the maximum size of the cache directory (in bytes). A value over the
* amount of available space on the disk will be reduced to the amount of
* available space. An unspecified default value will be applied. A value of 0
* will be ignored.
*
* \since LTO_API_VERSION=22

///

cdecl thinlto_codegen_set_cache_size_bytes(LLVMOpaqueThinLTOCodeGenerator* cg, uint max_size_bytes) -> void;
///
*
* Same as thinlto_codegen_set_cache_size_bytes, except the maximum size is in
* megabytes (2^20 bytes).
*
* \since LTO_API_VERSION=23

///

cdecl thinlto_codegen_set_cache_size_megabytes(LLVMOpaqueThinLTOCodeGenerator* cg, uint max_size_megabytes) -> void;
///
*
* Sets the maximum number of files in the cache directory. An unspecified
* default value will be applied. A value of 0 will be ignored.
*
* \since LTO_API_VERSION=22

///

cdecl thinlto_codegen_set_cache_size_files(LLVMOpaqueThinLTOCodeGenerator* cg, uint max_size_files) -> void;
///
*
* @} // endgroup LLVMCTLTO_CACHING

///

LLVM_C_EXTERN_C_END;
#endif;
///
LLVM_C_LTO_H
///
